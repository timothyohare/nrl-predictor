# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/
.pytest_cache/
.mypy_cache/

# CDK
infra/.venv/
infra/cdk.out/
*.js.map

# Environment
.env
.env.*
!.env.example

# AWS
.aws-sam/

# Frontend
frontend/node_modules/
frontend/.next/
frontend/.amplify-hosting/
frontend/out/

# IDE
.idea/
.vscode/
*.swp

# Spikes venv (already exists)
fetcher-spikes/venv/

# Mutation gate (CHG-0027) — mutmut working tree + per-run report are build artifacts
mutants/
reports/

# scripts/gather_round_context.py output — paste-ready prompt files, local only
manual_context/

# Coverage (pytest-cov / coverage.py) — data file + reports are build artifacts
.coverage
.coverage.*
coverage.xml
htmlcov/
frontend/coverage/

# Claude Code agent worktrees (isolation: worktree) — transient, harness-managed
.claude/worktrees/
